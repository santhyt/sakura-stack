# Note: black, isort, flake8 installed via pipx (not pip) to avoid
# pathspec version conflict with dbt-core. Install with:
#   pipx install black
#   pipx install isort
#   pipx install flake8

pymupdf==1.24.0
boto3==1.34.0
psycopg2-binary==2.9.9
python-dotenv==1.0.0
pandas==2.2.0
pydantic==2.6.0
apache-airflow==2.8.0
dbt-postgres==1.7.0